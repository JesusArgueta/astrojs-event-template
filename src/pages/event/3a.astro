---
const { id } = Astro.params;
import DotsButton from "../../components/DotsButton.astro";
import EventAddress from "../../components/EventAddress.astro";
import LikeButton from "../../components/LikeButton.astro";
import PageHeader from "../../components/PageHeader.astro";
import Layout from "../../layouts/Layout.astro";
import { allPlaylists } from "../../lib/data";
import PageFooter from "../../components/PageFooter.astro";

import BannerEvents from "../../components/BannerEvents.astro";
import { playlists, morePlaylists } from "../../lib/data";

export function getStaticPaths() {
  return allPlaylists.map((playlist) => ({
    params: {
      id: playlist.id,
    },
  }));
}

const playlist = allPlaylists.find((playlist) => playlist.id === "3");

// Importa el componente ONGCard
import ONGCard from "../../components/ONGCard.astro";

// Datos de las comida
const comidas = [
  {
    name: "Paella",
    description:
      "Deliciosa paella 100% vegetal con verduras frescas de temporada, especias aromáticas y el auténtico sabor mediterráneo. Un festín de colores y sabores que conquista hasta los paladares más exigentes. ¡Una opción saludable y llena de sabor! 💚",
    image: "/assets/images/comida/1.jpg",
    socialLinks: [],
  },
  {
    name: "Migas",
    description:
      "Deliciosas migas tradicionales reinventadas con verduras frescas, ajo, pimentón y aceite de oliva. Un plato reconfortante y lleno de sabor, 100% vegetal que rinde homenaje a la cocina de siempre. ¡Auténtica tradición con toque veggie! 🫒",
    image: "/assets/images/comida/2.jpg",
    socialLinks: [],
  },
  {
    name: "Garbanzos",
    description:
      "Deliciosos garbanzos cocinados con verduras frescas y especias aromáticas. Un plato lleno de proteína vegetal, sabor tradicional y nutrición. Rico, saludable y 100% vegetal. ¡Comfort food que alimenta cuerpo y alma! 💚",
    image: "/assets/images/comida/3.jpg",
    socialLinks: [],
  },
  {
    name: "Picoteo: patatas, altramuces, aceitunas",
    description:
      "Disfruta de un auténtico picoteo mediterráneo: patatas aliñadas, altramuces salados y aceitunas de la tierra. Sabores sencillos que evocan tradición y buena compañía. ¡El acompañamiento perfecto para cualquier momento! 🌞",
    image: "/assets/images/comida/4.jpg",
    socialLinks: [],
  },
  {
    name: "Cerveza, zumos, refrescos, rebujito",
    description:
      "Disfruta de cerveza fría, zumos, refrescos burbujeantes y el auténtico rebujito andaluz. ¡La combinación perfecta para refrescarte y disfrutar del evento! Bebidas para todos los gustos y momentos. 🍹✨",
    image: "/assets/images/comida/5.jpg",
    socialLinks: [],
  },
];
---

<Layout image={playlist?.cover} title={playlist?.title || "404"}>
  <div
    class="relative bg-zinc-900 min-h-full flex flex-col overflow-x-hidden"
    transition:name=`playlist ${playlist?.id} box`
  >
    <PageHeader />

    <!-- Información del evento -->
    <div
      class="flex flex-col items-center md:flex-row md:items-stretch gap-8 px-6"
    >
      <div class="h-52 w-52 flex-none">
        <img
          src={playlist?.cover}
          alt={playlist?.title}
          class="object-cover h-full w-full shadow-[5px_0_30px_0px_rgba(0,0,0,0.3)]"
          transition:name=`playlist ${playlist?.id} image`
        />
      </div>
      <div class="flex flex-col justify-center h-full">
        <div class="">
          <h1 class="title-clamp font-bold block mb-4">
            {playlist?.title}
            <span transition:name={`playlist ${playlist?.id} title`}></span>
          </h1>
          <p class="mt-2">
            {playlist?.description}
          </p>
        </div>
      </div>
    </div>

    <!-- Sección de comida -->
    <div class="px-6 py-12">
      <h2 class="text-4xl font-bold text-customYellow mb-8">Comida y Bebida</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        {comidas.map((comida) => <ONGCard {...comida} />)}
      </div>
    </div>

    <!-- <div class="px-6 py-12 text-center">
      <h2 class="text-4xl font-bold text-customYellow mb-6">
        Estamos trabajando en ello
      </h2>
      <p class="text-lg text-gray-300 mb-8">
        Estamos preparando esta sección para ti. Mientras tanto, disfruta de
        esta imagen de un perro gracioso 🐶.
      </p>
      <div class="flex justify-center">
        <img
          src="/assets/images/perro_trabajando.jpg"
          alt="Perro gracioso esperando"
          class="rounded-lg shadow-lg max-w-md"
        />
      </div>
    </div> -->

    <div
      class="absolute h-screen inset-0 z-[-1] bg-gradient-to-b"
      style=`--context-color:${playlist?.color.accent}`
    >
      <div
        class="absolute inset-0 bg-gradient-to-t via-transparent from-zinc-900"
      >
      </div>
    </div>
  </div>
  <PageFooter />
</Layout>

<style>
  .title-clamp {
    font-size: clamp(20px, 6vw, 70px);
    line-height: 1;
  }
</style>
