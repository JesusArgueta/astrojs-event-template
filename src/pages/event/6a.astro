---
const { id } = Astro.params;
import DotsButton from "../../components/DotsButton.astro";
import EventAddress from "../../components/EventAddress.astro";
import LikeButton from "../../components/LikeButton.astro";
import PageHeader from "../../components/PageHeader.astro";
import Layout from "../../layouts/Layout.astro";
import { allPlaylists } from "../../lib/data";
import PageFooter from "../../components/PageFooter.astro";

import BannerEvents from "../../components/BannerEvents.astro";
import { playlists, morePlaylists } from "../../lib/data";

export function getStaticPaths() {
  return allPlaylists.map((playlist) => ({
    params: {
      id: playlist.id,
    },
  }));
}

const playlist = allPlaylists.find((playlist) => playlist.id === "6");

// Importa el componente ONGCard
import ONGCard from "../../components/ONGCard.astro";

// Datos de las Sorteos
const sorteos = [
  {
    name: "Veggie Sevilla",
    description:
      "Gran donación! Una tablet nueva para niñas y un bolso Guess. Veggie Sevilla demuestra su compromiso social apoyando la educación y la infancia. ¡Gracias por vuestra solidaridad! 💚",
    image: "/assets/images/sorteos/4.png",
    socialLinks: [
    ],
  },
  {
    name: "Real Betis Balompié",
    description:
      "¡Gran sorteo! Gana una camiseta oficial del Betis firmada por los jugadores. Participa y consigue este recuerdo único de tu equipo del alma. ¡Mucha suerte, béticos! ⚪💚",
    image: "/assets/images/sorteos/2.png",
    socialLinks: [
    ],
  },
  {
    name: "Sevilla FC - Sorteo Exclusivo",
    description:
      "¡Sorteo especial! Gana una camiseta oficial del Sevilla FC firmada por toda la plantilla. Participa y llévate este recuerdo único de tus jugadores favoritos. ¡Mucha suerte, sevillistas! 🏆",
    image: "/assets/images/sorteos/3.png",
    socialLinks: [
    ],
  },
  {
    name: "Museo de las Ilusiones",
    description:
      "¡Participa en nuestro sorteo y gana entradas para el Museo de las Ilusiones! Vive una experiencia única llena de magia, diversión y percepciones sorprendentes. ¡No te lo pierdas! ✨",
    image: "/assets/images/sorteos/1.png",
    socialLinks: [
    ],
  },
  {
    name: "Hotty Doggy",
    description:
      "¡Premio especial! Cena romántica para 2 personas: entrante, 2 perritos de especialidad, 2 bebidas y postre a compartir. Disfruta de una velada deliciosa y diferente. ¡Perfecto para una cita inolvidable! 💫",
    image: "/assets/images/sorteos/5.png",
    socialLinks: [
    ],
  },
];
---

<Layout image={playlist?.cover} title={playlist?.title || "404"}>
  <div
    class="relative bg-zinc-900 min-h-full flex flex-col overflow-x-hidden"
    transition:name=`playlist ${playlist?.id} box`
  >
    <PageHeader />

    <!-- Información del evento -->
    <div
      class="flex flex-col items-center md:flex-row md:items-stretch gap-8 px-6"
    >
      <div class="h-52 w-52 flex-none">
        <img
          src={playlist?.cover}
          alt={playlist?.title}
          class="object-cover h-full w-full shadow-[5px_0_30px_0px_rgba(0,0,0,0.3)]"
          transition:name=`playlist ${playlist?.id} image`
        />
      </div>
      <div class="flex flex-col justify-center h-full">
        <div class="">
          <h1 class="title-clamp font-bold block mb-4">
            {playlist?.title}
            <span transition:name={`playlist ${playlist?.id} title`}></span>
          </h1>
          <p class="mt-2">
            {playlist?.description}
          </p>
        </div>
      </div>
    </div>

    <!-- Sección de ONGs -->
    <div class="px-6 py-12">
      <h2 class="text-4xl font-bold text-customYellow mb-8">Sorteos</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        {sorteos.map((sorteo) => <ONGCard {...sorteo} />)}
      </div>
    </div>

    <!-- <div class="px-6 py-12 text-center">
      <h2 class="text-4xl font-bold text-customYellow mb-6">
        Estamos trabajando en ello
      </h2>
      <p class="text-lg text-gray-300 mb-8">
        Estamos preparando esta sección para ti. Mientras tanto, disfruta de
        esta imagen de un perro gracioso 🐶.
      </p>
      <div class="flex justify-center">
        <img
          src="/assets/images/perro_trabajando.jpg"
          alt="Perro gracioso esperando"
          class="rounded-lg shadow-lg max-w-md"
        />
      </div>
    </div> -->

    <div
      class="absolute h-screen inset-0 z-[-1] bg-gradient-to-b"
      style=`--context-color:${playlist?.color.accent}`
    >
      <div
        class="absolute inset-0 bg-gradient-to-t via-transparent from-zinc-900"
      >
      </div>
    </div>
  </div>
  <PageFooter />
</Layout>

<style>
  .title-clamp {
    font-size: clamp(20px, 6vw, 70px);
    line-height: 1;
  }
</style>
