---
import PageHeader from "../components/PageHeader.astro";
import PageFooter from "../components/PageFooter.astro";

import BannerCard from "../components/BannerEvents.astro";
import { playlists, morePlaylists } from "../lib/data";

import HomePagePosts from "../components/Blog/HomePagePosts.astro";
import EventsTableMarcas from "../components/EventsTableMarcas.astro";
import Ticker from "../components/Ticker.astro";

import Layout from "../layouts/Layout.astro";

---

<Layout title="Home">
  <div
    id="playlist-container"
    class="relative transition-all duration-1000 bg-context"
    style="min-height:300px;--context-color:#000000;"
  >
    <PageHeader />
    
    <section class="text-center py-24 bg-black relative">
      <div class="absolute inset-0 w-full z-10  bg-black opacity-50"></div>
    
      <img src="/festival.jpg" alt="Background Image" class="absolute inset-0 w-full h-full object-cover opacity-40">
      <div class="relative z-10 max-w-4xl mx-auto">
        <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold text-customYellow">Colaboradores</h1>
        <p class="mt-4 text-sm md:text-base lg:text-lg text-gray-300 max-w-3xl mx-auto">
         El alma de este festival son sus colaboradores: corazones generosos que tejen esperanza con sus manos y su tiempo. Son el puente entre el abandono y un nuevo hogar, ángeles anónimos que convierten cada esfuerzo en alimento, cada sonrisa en medicina y cada gesto en un acto de amor puro por quienes no tienen voz. Esta celebración late gracias a su energía compasiva, recordándonos que la verdadera grandeza reside en alargar la mano para proteger a los más vulnerables.</p>
        <div class="mt-8 flex justify-center space-x-4">
        </div>
      </div>
      <!-- Thumbnails -->
      <div class="px-6 relative z-10 mt-12 max-w-7xl mx-auto">
  <div class="flex flex-wrap justify-center items-center mt-6 gap-4">
    {morePlaylists.map((playlist) => <BannerCard playlist={playlist} />)}
  </div>
</div>
    </section>

    <Ticker />
    
    <h2 class="text-4xl my-8 md:text-5xl lg:text-6xl font-bold text-white text-center mt-12">Marcas Financiadoras</h2>

    <EventsTableMarcas />

    <div class="mt-12 max-w-3xl mx-auto bg-black text-gray-300 px-8 py-10 rounded-lg shadow-lg">
      <p class="text-lg leading-relaxed">
        Estamos abiertxs a que otras marcas, entidades y comercios se unan a esta red solidaria. Si quieres que tu empresa forme parte del festival, puedes escribirnos a 
        <a href="mailto:lasonrisademayo@gmail.com" class="text-customYellow hover:text-yellow-400 font-bold">
          lasonrisademayo@gmail.com
        </a> 
        o rellenar directamente este formulario:
      </p>
      <div class="mt-6 text-center">
        <a
          href="/contact"
          class="bg-customYellow text-black font-bold py-3 px-8 rounded-lg hover:bg-customYellowHover transition duration-300 inline-block"
        >
          REGÍSTRATE!!
        </a>
      </div>
      <p class="mt-6 text-lg leading-relaxed">
        Tu apoyo nos ayuda a dar visibilidad al bienestar animal y a seguir cuidando de los peludos de La Sonrisa Animal (LASA) 🐾.
      </p>
    </div>
  

  <PageFooter />
  
</Layout>
